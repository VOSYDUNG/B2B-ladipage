# Policy asset placeholder

The uploaded package contains only `/public/images/page-1.png` as a policy preview.
Before production, add the approved PDF here and update the policy links in the UI.
Do not expose the placeholder QR from the preview as a live CTA.
