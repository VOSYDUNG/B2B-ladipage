# Content governance

- `safeContent` is the default production copy.
- `pendingMedicalClaims` is review-only and must not be published until Regulatory/Medical approval.
- Lao UI translation is intentionally not auto-generated. The current build uses Vietnamese copy plus the approved-looking product name from supplied packaging references.
